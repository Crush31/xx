<!doctype html>
<html lang="zh-CN">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Happy Birthday</title>
    <link rel="stylesheet" type="text/css" href="../css/styles.css">
</head>

<body>
    <div class="htmleaf-container">
        <div class="wrapper">
            <div class="container">
                <h1>生日快乐!</h1>
                <form class="form" id="loginForm">
                    <input id="userName" name="userName" type="text" placeholder="姓名" required>
                    <input id="pwd" name="pwd" type="text" placeholder="生日 (6位数字，如: 010101)" required>
                    <button type="submit" id="login-button">进入</button>
                </form>
            </div>
            <ul class="bg-bubbles">
                <li></li><li></li><li></li><li></li><li></li>
                <li></li><li></li><li></li><li></li><li></li>
            </ul>
        </div>
    </div>

    <script src="../js/jquery-2.1.1.min.js"></script>
    <script>
        $(document).ready(function() {
            console.log("页面加载完成，脚本已启动");
            
            $('#loginForm').on('submit', function(e) {
                e.preventDefault();
                console.log("表单提交事件触发");
                
                var userName = $('#userName').val().trim();
                var pwd = $('#pwd').val().trim();
                
                console.log("用户名:", userName);
                console.log("密码原始值:", pwd);
                console.log("密码长度:", pwd.length);
                console.log("是否为6位数字:", /^\d{6}$/.test(pwd));
                
                if (/^\d{6}$/.test(pwd)) {
                    console.log("验证通过，准备跳转");
                    // 先跳转到 Cl.html 测试
                    var targetUrl = '../Cl.html?name=' + encodeURIComponent(userName);
                    console.log("跳转URL:", targetUrl);
                    window.location.href = targetUrl;
                } else {
                    console.log("验证失败");
                    alert('请输入正确的6位生日数字（例如：010101）');
                }
            });
        });
    </script>
</body>

</html>
